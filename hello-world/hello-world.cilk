#include<stdio.h>

cilk void print(int i)
{
    if(i == 0)
        return;

    spawn print(i-1);
    spawn print(i-1);
    sync; 
    printf("%d Hello, World\n", i);
}

cilk int main()
{
    spawn print(5);
	return 0;
}
